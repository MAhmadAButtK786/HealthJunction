import 'package:flutter/material.dart';

class HealthZone extends StatelessWidget {
  const HealthZone({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
